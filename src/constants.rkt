#lang racket

(provide UNIT-SIZE
         MODE
         COLOR
         BG-COLOR)

;; Default unit, colors, and style
(define UNIT-SIZE 100)
(define MODE 'solid)
(define COLOR "cornflower blue")
(define BG-COLOR 'lavender)
